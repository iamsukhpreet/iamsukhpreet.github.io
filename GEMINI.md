# Project Instructions

## General Guidelines

- **Environment Awareness**: Before invoking platform-specific scripts or shell commands, always check the current Operating System and terminal/environment (e.g., Windows PowerShell vs. Bash). Ensure that commands use the correct syntax for the detected environment (e.g., using `;` or `&&` correctly as separators).
